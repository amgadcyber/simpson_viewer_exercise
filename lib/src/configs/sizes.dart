/* DEFAULT SIZES */
import 'package:get/get.dart';

// - Screen Sizes
final width = Get.width;
final height = Get.height;

// - Original
const collapsedHeight = 70.0;
const expandedHeight = 200.0;

/// -- Paddings
const double smPadding = 5.0;

/// -- Icon Sizes
const double defaultIconSize = 20.0;

/// -- Radius and effects Values
const double defaultRadius = 10;
